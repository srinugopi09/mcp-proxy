# Core dependencies - use `uv sync` for better dependency management
# Or install with: pip install -e .

fastmcp>=2.0.0
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Optional test dependencies
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# httpx>=0.24.0